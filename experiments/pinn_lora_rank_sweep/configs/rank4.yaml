# LoRA Rank Sweep — rank=4
# Auto-generated by harness-engineering plan
# Date: 2026-06-28

model:
  lora_rank: 4
  base_hidden_dim: 128
  base_num_layers: 4
  activation: tanh

training:
  seed: 42
  epochs: 1000
  lr: 0.001
  lr_scheduler: cosine
  weight_decay: 0.0
  batch_size: 128

pde:
  type: burgers           # TODO: replace with target PDE
  nu: 0.01                 # viscosity / PDE parameter

logging:
  wandb: false
  log_interval: 10
  save_every: 100
  output_dir: outputs/rank4_seed42
  checkpoint_dir: checkpoints/rank4_seed42

data:
  n_train: 10000
  n_val: 2000
  collocation_points: 20000
